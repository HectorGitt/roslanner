{
  "name": "noone",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "//dev": "No prisma generate here: on Windows it can't replace the query engine DLL while another dev server holds it, which stops dev starting. postinstall covers a fresh clone, build covers CI.",
    "dev": "next dev",
    "//build": "Generate explicitly: npm no longer runs dependency install scripts by default, so Prisma's postinstall can't be relied on to produce the client on a build host.",
    "build": "prisma generate && next build",
    "start": "next start",
    "lint": "eslint",
    "postinstall": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:seed": "node prisma/seed.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "better-auth": "^1.6.23",
    "next": "16.2.10",
    "prisma": "^6.19.3",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.10",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
